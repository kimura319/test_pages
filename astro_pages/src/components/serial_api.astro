<!-- Web Serial API セクション -->
<div class="serial-container">
    <h2>Web Serial API</h2>
    
    <div class="serial-controls">
        <button id="connectButton" class="serial-button">シリアル接続</button>
        <button id="disconnectButton" class="serial-button" disabled>切断</button>
        <button id="sendButton" class="serial-button">送信</button>
    </div>
    
    <div class="serial-status">
        <p>ステータス: <span id="status">未接続</span></p>
    </div>
</div>

<style>
    .serial-container {
		max-width: 800px;
		margin: 40px auto;
		padding: 20px;
		border: 2px solid #e1e5e9;
		border-radius: 12px;
		background-color: #ffffff;
	}

	.serial-container h2 {
		color: #2A233E;
		font-size: 24px;
		font-weight: bold;
		margin-bottom: 30px;
		text-align: center;
	}

	.serial-container h3 {
		color: #2A233E;
		font-size: 18px;
		font-weight: bold;
		margin: 20px 0 10px 0;
	}

	.serial-controls {
		display: flex;
		gap: 10px;
		margin-bottom: 20px;
		justify-content: center;
	}

	.serial-button {
		padding: 10px 20px;
		background-color: #FF7E33;
		color: white;
		border: none;
		border-radius: 8px;
		font-size: 16px;
		font-weight: bold;
		cursor: pointer;
		transition: background-color 0.3s ease;
	}

	.serial-button:hover:not(:disabled) {
		background-color: #e6691a;
	}

	.serial-button:disabled {
		background-color: #9ca3af;
		cursor: not-allowed;
	}

	.serial-status {
		text-align: center;
		margin-bottom: 20px;
		padding: 10px;
		background-color: #f9fafb;
		border-radius: 8px;
	}

	.serial-status p {
		margin: 0;
		color: #2A233E;
		font-weight: bold;
	}

	.serial-status span {
		color: #FF7E33;
	}
</style>